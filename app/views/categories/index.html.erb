<!-- <marquee scrollamount = "10" bgcolor = "#aed00d" width="1260" height="60" behavior="scroll" style="border:solid">ASHISH RAO<br>DCT ACADEMY<br>RAILS SERVER</marquee> -->
<hr>
<h2>Listing  Categories : <%=@categories.length %></h2>

<% if @categories.empty? %>
<p> Categories have not been added yet</p>
<% else %>
<ol>
  <% @categories.each do |category| %>
  <li><%= link_to category.name, category_path(category.id) %>
    (<%= Product.where('category_id = ?', category.id).length %>)
  </li>
  <% end %>
</ol>
<% end %>
<hr>
<a href="http://localhost:3000/categories/new" class="btn btn-info" role="button">Add Category</a>
<%= link_to "", new_category_path, target: "_blank" %>
<!-- new_category_path return string "/categories/new" -->
<!--
<%= link_to "Add Category", "/categories/new" %>
-->
